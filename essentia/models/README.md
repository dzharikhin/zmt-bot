# Models download
https://essentia.upf.edu/models.html
most of the models are two step:
1. Embeddings
2. Features
> try to minimize amount of different embeddings required to obtain features. For example try to get models based on `musicnn` embeddings, so same embeddings can be reused across the models